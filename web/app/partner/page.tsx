import { PartnerClient } from "./PartnerClient";

export const metadata = {
  title: "MONO 기업 파트너",
  description: "채용 공고를 먼저 등록하고 검증된 현장 기술자를 우선 안내받으세요.",
};

export default function PartnerPage() {
  return <PartnerClient />;
}
