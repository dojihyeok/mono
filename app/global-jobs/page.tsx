import GlobalJobsClient from './GlobalJobsClient';

export const metadata = {
    title: 'Global Matching | MO-NO Masters',
    description: '호주, 캐나다, 독일 등 선진국 프리미엄 현장 매칭. 실시간 환율 계산 및 글로벌 비자 지원 프로젝트 관리.',
};

export default function GlobalJobsPage() {
    return <GlobalJobsClient />;
}
