import ReportClient from '../ReportClient';

export const metadata = {
    title: 'ISO Standard Career Report | MO-NO Masters',
    description: '국제 표준(ISO) 기술 경력 인증 리포트. 호주, 캐나다, 독일 대사관 공식 경력 증빙 패스포트 엔진.',
};

export default function ReportPage() {
    return <ReportClient />;
}
